const nextConfig = {
    images:{
        remotePatterns: [
            {
                hostname: "image.tmdb.org"
            },
            {
                hostname: "localhost"
            },
            {
                hostname: "jio-cinema-server.onrender.com"
            }
        ]
    }
};

export default nextConfig;
